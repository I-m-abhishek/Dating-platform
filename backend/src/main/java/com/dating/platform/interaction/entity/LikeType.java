package com.dating.platform.interaction.entity;

public enum LikeType {
    /** Ordinary like. */
    STANDARD,
    /** Priority like - surfaces at the top of the recipient's Likes tab. */
    SUPER
}
